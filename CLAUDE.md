# 會幕拆卸(hfpc-tabernacle-screw)

聖經版「螺絲解謎」:照民數記 4 章的次序把會幕一層層拆下來,橛子按名分進三族的擔子。
零相依、零美術檔、可離線(PWA)、DOM/CSS 渲染,不用 Canvas 也不用物理引擎。

## 現況(2026-07-25 建立;同日晚加難度+美術)

- 6 關,全部經 solver 判定**保證可解**(三個年齡檔各驗)、瀏覽器實測真的點得完。
- **已上架**:GitHub `summer09201017-cloud/hfpc-tabernacle-screw` ·
  CF **Pages** `hfpc-tabernacle-screw.pages.dev` · 已進大廳 data.js 卡片 · sites.json 已登記 ·
  worker NAMES 已加。**✅ 2026-07-26 牧者過審文案與神學語氣,大廳卡片已點亮**,可以推給老師上課用。
- 部署一律 `node scripts/build-dist.mjs` 再 `wrangler pages deploy dist`(白名單制)。
  ★ 不要 deploy 專案根目錄:那會把 `scripts/` 一起送上 CDN;
  **Pages 不吃 `.assetsignore`**(那是 Workers assets 的功能)。
- 難度:幾何改由 `layout.js` 依「疊柱規格」算出來(手擺座標擺不出真遮擋 → 第一版會想 bot 全關 0% 卡死)。
  現況青少年檔會想 bot 卡死:站1 教學 0% · 站2 23% · 站3 20% · 站4 19% · 站5 26% · 站6 31%。
  ⚠ 改 `columns` / `bins` 就是改難度,改完必跑 `npm run check`(難度出帶會 exit 1)。
  要重新挑分法用 `npm run tune`(窮舉分法 × 擔子數,並硬篩可解性、a11y 間距、神學次序)。

## 玩法規則(只有一份:`rules.js`)

- **螺絲=橛子**;上面還有沒拆掉的板壓著就拔不出來(民 4:5-6 的拆卸順序)。
- 拔下來的橛子自動進**擔子**:先找同色沒滿的,沒有就開空擔子,都沒有 → **卡住**。
- 一個擔子滿 3 個同色就被扛走(清空)。板上螺絲拔光 → 板子掉落,露出下面。
- 顏色即經文:藍/紫/朱紅=幔子三色(出 26:1)、木=米拉利的板閂柱座、金=哥轄的聖所器具。

> ★ 規則只准有一份。solver 與遊戲**同時 import `rules.js`**——
> 若 solver 自己抄一份,會出現「solver 說可解、孩子課堂上卡死」。

## 出廠檢驗(改關卡後**必跑**)

```bash
npm run check          # lint + 逐關求解 + 難度量測;不通過 exit 1
npm run check -- --path   # 順便印通關順序
npm run serve          # 另開一個終端
npm run test:browser   # 真開瀏覽器從第一關玩到全破
```

三道關卡設計鐵則(solver 會擋):

1. **每種顏色的螺絲總數必須是 3 的倍數** —— 否則一定有湊不滿的殘留 → 必卡死。
2. 每一關都要 solver 判定「保證可解」(窮舉 + 記憶化,不是抽樣)。
3. 每根螺絲要落在自己那片板子範圍內(否則畫面上浮在半空點不到)。

### 難度怎麼看(兩隻 bot)

- **躺平 bot**:完全不看顏色亂拆 → 代表「不動腦的下場」。
- **會想 bot**:優先拿「已開擔子的同色」→ 逼近一個會想一下的孩子。
- 出廠標準:**會想 bot 幾乎不卡(不勸退)、躺平 bot 會吃苦頭(有挑戰)**。

> ⚠ 只看躺平 bot 會系統性高估難度。第一版就是這樣把 55% 誤判成「太緊」,
> 差點把好關卡砍掉。難度的槓桿是**擔子數 vs 顏色種類**:
> 顏色種類 ≤ 擔子數 → 永遠有空盒 → 不可能卡 → 零挑戰(第一版三色三擔子量到 0%)。

## 目前難度曲線

| 關 | 螺絲 | 顏色 | 擔子 | 躺平卡死 | 會想卡死 |
|---|---|---|---|---|---|
| 1 院子的帷子 | 6 | 2 | 3 | 0%(教學) | 0% |
| 2 會幕的門簾 | 9 | 3 | 3 | 0%(教學) | 0% |
| 3 帳幕的蓋 | 12 | 4 | 3 | 68% | 0% |
| 4 帳幕的板與閂 | 15 | 5 | 4 | 55% | 0% |
| 5 柱子和帶卯的座 | 18 | 5 | 4 | 57% | 0% |
| 6 幔子與法櫃的杠 | 18 | 5 | 4 | 55% | 0% |

## 統計打點

`tabernacle-screw` 開啟(10 分去重)/ `-done` 完賽 / `-dwell` 真實停留 / `-stuck` 卡住。
遊戲層零個資。★ 沒有 `-dwell` 會被全域 hook #18 dwell-beacon-guard 擋下部署。

## 待辦

- [ ] 上架(要使用者點名):建 GitHub repo → CF Pages → 進大廳 data.js → 登記 sites.json + worker NAMES
- [ ] 加關(每加一關都要重跑 `npm run check`)
- [ ] 可考慮:關卡地圖 / 年齡檔 / 過關彩帶(win-confetti)/ 朗讀經文(web-speech-scripture)
